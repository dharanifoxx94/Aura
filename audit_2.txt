
gemini --file eidolon-vault-steps-2-3-4.md "Apply ALL the changes and suggestions in this document exactly. Update or create every file as instructed and give me the FULL updated code for every changed or new file at the end."
```

---

```markdown
# EIDOLON-VAULT STEPS 2+3+4 UPGRADE DIRECTIVES
**Project:** Eidolon-Vault (persistent agentic consciousness vault)
**Current State:** Ollama local support is now added (Step 1 complete)
**Goal:** Add real persistence (Step 2), a killer demo scenario (Step 3), and polish README + demo folder (Step 4) so the project feels alive and ready for viral posts.

**IMPORTANT RULES FOR YOU (Gemini):**
- Keep everything lightweight for a 2015 laptop (16GB RAM, 2-core CPU).
- Use simple, clean code — no heavy dependencies.
- After applying, output the FULL code of every changed or new file at the end.
- Do not add extra features beyond these three steps.

────────────────────────────────────────
### STEP 2: REAL PERSISTENCE (SQLite + ChromaDB)

**Why:** Agents must remember across runs and build long-term "consciousness" memory.

**Tasks:**
1. Update requirements.txt to add:
   - chromadb
   - sqlalchemy

2. Create new file: eidolon_vault/persistence.py
   (Use SQLite for structured memory + ChromaDB for vector embeddings)

3. Update the core engine to use this persistence layer when an agent is created.

────────────────────────────────────────
### STEP 3: KILLER DEMO SCENARIO

**Create a new folder:** demo/
**Inside it create:** consciousness_debate.py

This demo should:
- Create 2 agents (one "optimist", one "skeptic") debating AI consciousness
- Run for 10 simulated "days"
- Save memory to persistence
- Generate a final "Trajectory Report" (markdown file)

Add a CLI command: `eidolon-vault demo consciousness`

────────────────────────────────────────
### STEP 4: POLISH README + DEMO FOLDER

**Update README.md** with these new sections:
- Local-First Mode (Ollama)
- Persistence & Memory
- Running the Consciousness Debate Demo
- Performance on old hardware (E5450 notes)
- Screenshots section (placeholder for future)

Also add a simple demo/README.md with instructions.

────────────────────────────────────────
### EXACT CODE TO IMPLEMENT

**1. Update requirements.txt** (replace entire file):

```txt
pyyaml
rich
tqdm
google-generativeai
groq
openai
ollama>=0.4.0
beautifulsoup4
pypdf
chromadb
sqlalchemy
```

**2. New file: eidolon_vault/persistence.py** (create exactly):

```python
# eidolon_vault/persistence.py
import chromadb
from sqlalchemy import create_engine, text
from pathlib import Path

class EidolonMemory:
    def __init__(self, agent_id: str):
        self.agent_id = agent_id
        self.db_path = Path("data") / f"{agent_id}.db"
        self.db_path.parent.mkdir(exist_ok=True)

        self.engine = create_engine(f"sqlite:///{self.db_path}")
        self.vector_client = chromadb.PersistentClient(path="data/chroma")
        self.collection = self.vector_client.get_or_create_collection(name=agent_id)

    def save_memory(self, text: str, metadata: dict = None):
        with self.engine.connect() as conn:
            conn.execute(text("INSERT INTO memories (content, timestamp) VALUES (:text, datetime('now'))"),
                        {"text": text})
        if metadata:
            self.collection.add(documents=[text], metadatas=[metadata], ids=[str(hash(text))])

    def get_recent_memories(self, limit: int = 20):
        with self.engine.connect() as conn:
            result = conn.execute(text("SELECT content FROM memories ORDER BY timestamp DESC LIMIT :limit"),
                                 {"limit": limit})
            return [row[0] for row in result]
```

**3. Update your main agent/engine file** (where agents are created) to use persistence.

**4. New file: demo/consciousness_debate.py** (create exactly):

```python
# demo/consciousness_debate.py
from eidolon_vault.core import Agent
from eidolon_vault.persistence import EidolonMemory

def run_consciousness_debate(days: int = 10):
    optimist = Agent("optimist", persistence=EidolonMemory("optimist"))
    skeptic = Agent("skeptic", persistence=EidolonMemory("skeptic"))

    for day in range(1, days+1):
        print(f"\n=== Day {day} ===")
        optimist.think(f"Day {day}: Why I believe I might be conscious...")
        skeptic.think(f"Day {day}: Why this is just clever simulation...")

    report = optimist.generate_trajectory_report()
    with open("demo/trajectory_report.md", "w") as f:
        f.write(report)
    print("✅ Trajectory Report saved to demo/trajectory_report.md")
```

**5. Update README.md** (add these sections at the end):

```markdown
## 🚀 Local-First Mode (Ollama)
```bash
eidolon-vault run --provider ollama --model llama3.2:3b
```

## 🧠 Persistence & Memory
Agents now have long-term memory using SQLite + ChromaDB.

## 🎭 Demo: Consciousness Debate
```bash
eidolon-vault demo consciousness
```
Runs 10 simulated days of debate and generates a trajectory report.

## Performance on Old Hardware
Tested on 2015 Dell E5450 (i5-5300U, 16GB RAM) — runs smoothly with gemma2:2b or llama3.2:3b.
```

**End of directives.**

**Final Instructions for Gemini:**
- Apply Steps 2, 3, and 4 exactly as described.
- Create all new files and folders.
- Update existing files cleanly.
- At the very end, output the FULL content of every changed or newly created file so I can copy-paste them directly into my repo.
- Do not add any extra features.

Apply now.
```

---

