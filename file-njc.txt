# EIDOLON-VAULT FINAL FIXES (Steps 2–3–4 Completion)

**Repository state:**  
You are working on the `eidolon-vault` project.  
The repo was freshly pulled on **15 Mar 2026**, at commit **bdf9e84**.  
Assume that the basic project structure already exists, including an `eidolon_vault` Python package and a `demo/` directory.

Your task is to **finish Steps 2, 3, and 4**:

- Make persistence actually work (SQLite + ChromaDB).
- Make the consciousness debate demo clean and runnable.
- Wire persistence into the core `Agent` class.
- Add a CLI entry point for the demo.
- Update the README so users understand how to run everything, including on old hardware.

You must **apply every directive below exactly** and **output the full, final contents of every changed or newly created file** at the end.

---

## GLOBAL RULES (READ FIRST, THEN FOLLOW)

1. **Do not improvise new features.**  
   Only do what is explicitly requested in this document.

2. **When a step says “replace entire file”, you must:**
   - Overwrite that file completely with the provided content or structure.
   - Not preserve any old code in that file (unless explicitly told to).

3. **When a step says “update”, you must:**
   - Modify the existing file in-place.
   - Keep unrelated functionality intact.
   - Only add or adjust what is required for persistence + demo + CLI + README.

4. **At the end of your work, you must output the complete, final source code of every file you changed or created.**
   - Include each file path as a heading or clear label.
   - Then include the **full file contents** (not a diff, not partial snippets).
   - Do not omit any changed file.

5. **Assume this is a local-only Python project** that will be run via a CLI entry point like `eidolon-vault ...`.

---

## 1. Update `requirements.txt` (REPLACE ENTIRE FILE)

**File:** `requirements.txt`  
**Action:** Replace the entire file contents with exactly the following list (one requirement per line, in this order):

```txt
pyyaml
rich
tqdm
google-generativeai
groq
openai
ollama>=0.4.0
beautifulsoup4
pypdf
chromadb
sqlalchemy
```

Notes:

- Ensure there are **no extra spaces, comments, or blank lines**.
- This is the **complete** set of required dependencies for now.
- The goal is to make sure that both the persistence layer (SQLite + ChromaDB) and the rest of the project have all their dependencies available.

---

## 2. Ensure `eidolon_vault/persistence.py` Is Correct

**File:** `eidolon_vault/persistence.py`  
**Current state:** This file already exists and was created previously by Gemini. It should define an `EidolonMemory` class and handle SQLite + ChromaDB.

**Goal:** Confirm that this file is complete and self-contained. If anything is missing or incorrect, fix it. If it’s already correct, you may keep it as-is.

**Requirements for `EidolonMemory`:**

- The file must have all necessary imports:
  - For example, imports for `sqlite3` (or SQLAlchemy, if you are using it consistently), filesystem operations, and `chromadb`.
- On initialization, `EidolonMemory` must:
  - Ensure that the underlying SQLite database and table exist.
  - Ensure that the ChromaDB collection exists.
- The class must support at least:
  - A `save_memory(...)` method that:
    - Stores memory entries in SQLite.
    - Stores corresponding embeddings / vectors in the ChromaDB collection.
  - A `get_recent_memories(limit: int = N)` method that:
    - Returns recent memory entries in a reasonable format (e.g., list of rows or objects), ordered by recency.
- The implementation must **not** crash when brought into a fresh repo state where only the declared dependencies exist.
- The implementation must not assume that directories already exist (directory creation is handled in a later step).

**Action:**

- If `eidolon_vault/persistence.py` already meets these requirements, you may leave it unchanged.
- If it does not, adjust it so it:
  - Has correct imports.
  - Creates tables and collections on first use.
  - Implements `save_memory` and `get_recent_memories` correctly.

At the end, include the **full content** of `eidolon_vault/persistence.py` in your final output.

---

## 3. Wire Persistence Into the Main Agent/Core Class

**Goal:** The `Agent` objects must be able to use `EidolonMemory` for long-term memory.

**Task:**

1. Locate the Python file where the main `Agent` class is defined.  
   - This is most likely `eidolon_vault/core.py` or `eidolon_vault/agent.py`.  
   - If there is an `Agent` class elsewhere that is clearly the primary one used by the system, use that.

2. Modify the `Agent` class `__init__` to accept a `persistence` argument and wire it up.

   - If `__init__` already accepts `persistence`, ensure it is correctly stored.
   - If it does **not** accept `persistence`, add a parameter with a default of `None`.

3. Inside `__init__`, add or update the line:

```python
self.persistence = persistence or EidolonMemory(self.name)
```

**Important details:**

- Make sure you import `EidolonMemory` at the top of this file:

```python
from eidolon_vault.persistence import EidolonMemory
```

- `self.name` should be the agent’s identifier (e.g., a string passed to the constructor like `"optimist"` or `"skeptic"`).
- Do **not** break existing code paths that create `Agent` without explicitly passing `persistence`.
  - They should still work by automatically creating an `EidolonMemory` instance.

4. If the `Agent` class already has methods that conceptually represent:
   - thinking,
   - logging interactions,
   - or generating trajectory reports,
   then make sure those methods **use** `self.persistence` where appropriate (if not already handled by the rest of the code). For this pass, it’s acceptable if only the initialization wiring is guaranteed, as long as you do not introduce new bugs.

At the end, output the full, updated file that defines `Agent` (e.g., `eidolon_vault/core.py` or `eidolon_vault/agent.py`).

---

## 4. Fix `demo/consciousness_debate.py` (REPLACE ENTIRE FILE)

**File:** `demo/consciousness_debate.py`  
**Problem today:**  
- Imports are crammed into a single line and contain syntax errors.  
- There is a messy `sys.path` hack.  
- The code tries to pass `persistence=` to `Agent` while the core wasn’t wired yet.  
- The demo is not runnable as a clean Python module.

**Goal:** Replace this file with a clean, minimal, fully runnable demo that:

- Uses the `Agent` class from the core module.
- Uses `EidolonMemory` explicitly.
- Runs a fixed number of “days” of debate.
- Writes a trajectory report to `demo/trajectory_report.md`.

**Action (replace the entire file with exactly this content, adjusting only for formatting if required):**

```python
# demo/consciousness_debate.py

import os

from eidolon_vault.core import Agent
from eidolon_vault.persistence import EidolonMemory


def run_consciousness_debate(days: int = 10) -> None:
    """
    Run a multi-day debate between two agents about consciousness.
    Results in a trajectory report written to demo/trajectory_report.md.
    """

    # Create two agents, each with their own persistent memory store.
    optimist = Agent("optimist", persistence=EidolonMemory("optimist"))
    skeptic = Agent("skeptic", persistence=EidolonMemory("skeptic"))

    for day in range(1, days + 1):
        print(f"\n=== Day {day} ===")

        optimist.think(f"Day {day}: Why I believe I might be conscious...")
        skeptic.think(f"Day {day}: Why this is just clever simulation...")

    # After the debate concludes, generate a trajectory report.
    report = optimist.generate_trajectory_report()

    # Ensure the demo directory exists.
    os.makedirs("demo", exist_ok=True)

    # Save the report to a markdown file.
    report_path = os.path.join("demo", "trajectory_report.md")
    with open(report_path, "w", encoding="utf-8") as f:
        f.write(report)

    print(f"Trajectory Report saved to {report_path}")


if __name__ == "__main__":
    run_consciousness_debate()
```

**Requirements:**

- No `sys.path` hacks.
- Only standard, explicit imports.
- The `Agent` class must accept the `persistence` argument (from Step 3).
- Assume that:
  - `Agent.think(...)` is a valid method.
  - `Agent.generate_trajectory_report()` is a valid method.
- If any of these methods do not exist, create thin wrappers or minimal implementations in the `Agent` class so that this demo can run without crashing (do not add extra features beyond what’s needed).

At the end, output the full final content of `demo/consciousness_debate.py`.

---

## 5. Add CLI Command for the Consciousness Demo

**File:** `eidolon_vault/cli.py` (or the primary CLI entry file for this project)

**Goal:** Provide a command so that a user can run:

```bash
eidolon-vault demo consciousness
```

and it will execute `run_consciousness_debate()` from the demo module.

**Action:**

1. Locate the existing CLI argument parsing logic:
   - It may use `argparse`, `click`, or another library.
   - Identify where `args.command` and `args.subcommand` (or equivalent) are handled.

2. Add logic such that when:

- `command == "demo"` **and**
- `subcommand == "consciousness"`

the CLI:

- Imports the function:

  ```python
  from demo.consciousness_debate import run_consciousness_debate
  ```

- Calls:

  ```python
  run_consciousness_debate()
  ```

**Concrete snippet to integrate (adapt paths only if necessary):**

```python
if args.command == "demo" and getattr(args, "subcommand", None) == "consciousness":
    from demo.consciousness_debate import run_consciousness_debate
    run_consciousness_debate()
```

Notes:

- If your CLI uses a different variable than `args.subcommand`, adapt the conditional to match your actual parser.
- Do **not** remove or break any existing commands.
- Ensure that this code path is reachable from the `eidolon-vault` console script / entry point.

At the end, output the full, updated CLI file (e.g., `eidolon_vault/cli.py`).

---

## 6. Ensure `data/` Folder Creation for Persistence

**Goal:** The persistence layer expects a `data/` directory for its database or related files. Right now, if this directory does not exist, the code will fail on first run.

**Action:**

1. Decide **where** the `data/` folder should be created. Acceptable options (pick one and be consistent):

   - On initialization of `EidolonMemory` in `eidolon_vault/persistence.py`.  
   - Or early in application startup (e.g., in the CLI before agents are created).

2. Implement directory creation using:

```python
os.makedirs("data", exist_ok=True)
```

or an equivalent call, **before** any attempt to write SQLite DB files or ChromaDB data into that directory.

3. Make sure that:

   - You import `os` where needed.
   - The path is consistent with how the persistence layer refers to its database (e.g., `data/memory.db` or similar).

At the end, the application should **not crash** due to a missing `data/` directory on first run.

If you make changes to `eidolon_vault/persistence.py` or any other file to support this, remember to include the **full final content** of those files in your output.

---

## 7. Update `README.md` (REPLACE SECTIONS AFTER “Quick Start”)

**File:** `README.md`

**Goal:** Keep everything up to and including the **“Quick Start”** section unchanged, but replace all content **after** “Quick Start” with the following sections **exactly** (structure and content must match, except for Markdown formatting details if necessary).

**Action:**

1. Locate the “Quick Start” section in `README.md`.
2. Keep “Quick Start” and everything above it as-is.
3. Delete everything **after** “Quick Start”.
4. Insert the following content in its place:

```markdown
## Local-First Mode (Ollama)

You can run eidolon-vault completely locally using [Ollama](https://ollama.com/).

First, pull a small, efficient model:

```bash
ollama pull llama3.2:3b
```

Then run eidolon-vault with Ollama as the provider:

```bash
eidolon-vault run --provider ollama --model llama3.2:3b
```

## Persistence & Memory

Agents now support **long-term memory** using a combination of **SQLite** and **ChromaDB**.

- Each agent can be initialized with a dedicated `EidolonMemory` instance.
- Interactions can be stored and later retrieved as “recent memories”.
- Memory data is stored under a local `data/` directory, so it stays on your machine.

## Demo: Consciousness Debate

A built-in demo simulates a multi-day debate between two agents:

```bash
eidolon-vault demo consciousness
```

This will:

- Run 10 simulated “days” of discussion between an **Optimist** and a **Skeptic** agent.
- Use persistence so each agent can build on previous days’ memories.
- Generate a trajectory report at:

  ```text
  demo/trajectory_report.md
  ```

## Performance on Old Hardware

This project is designed to be usable on older laptops and desktops.

Example tested configuration:

- **Machine:** 2015 Dell E5450  
- **CPU:** Intel i5-5300U  
- **RAM:** 16GB  
- **Mode:** Offline / Local-first  
- **Models:** `gemma2:2b` or `llama3.2:3b` (via Ollama)

On this hardware, the system runs smoothly in local-first mode when using small models like `gemma2:2b` or `llama3.2:3b`.
```

At the end, output the full final content of `README.md`.

---

## 8. Final Output Requirements

After applying all the changes above, you must output the **complete, final source code** of every file you modified or created.

At minimum, this should include:

1. `requirements.txt`
2. `eidolon_vault/persistence.py`
3. The file defining `Agent` (e.g., `eidolon_vault/core.py` or `eidolon_vault/agent.py`)
4. `demo/consciousness_debate.py`
5. `eidolon_vault/cli.py` (or the primary CLI file)
6. `README.md`

For each file:

- Print a clear header with the relative path, for example:

  ```text
  === requirements.txt ===
  ```

- Then print the **full contents** of that file.

Do **not** show diffs.  
Do **not** omit any part of the updated files.  
Do **not** include commentary between file contents unless clearly separated.

---

**End of directives.**  
Apply all changes now and then return the **full updated contents** of every changed or new file.  