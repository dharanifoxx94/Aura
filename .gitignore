# Secrets & config (may contain API keys)
*config*.yaml
*.yaml.bak
.env

# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/

# Virtual environments
psie-env/
venv/
.venv/

# Eidolon Vault runtime data (relative paths — tilde doesn't work in gitignore)
data/
eidolon_vault_reports/
*.db
.eidolon_vault/

# Logs
*.log
