gemini --file eidolon-vault-ollama-upgrade.md "Apply all the changes and suggestions in this document to my project. Update the files exactly as instructed and give me the full updated code for every changed/new file."```
---
```markdown# EIDOLON-VAULT OLLAMA UPGRADE DIRECTIVES**Project:** Eidolon-Vault (your persistent agentic consciousness vault)**Goal:** Add full local-first Ollama support so it runs 100% offline on your 2015 Dell E5450**Priority:** Make it clean, minimal, and production-ready for your hardware
## CURRENT PROJECT STATE (from your repo)- You have a clean Python structure with providers for Gemini/Groq/OpenRouter- Core engine exists but lacks local Ollama support- No offline mode yet- You want this to run smoothly on 16GB RAM + custom 7.0-rc2 kernel
## ALL CHANGES TO APPLY (in order)
### 1. Update requirements.txtReplace the entire file with this:
```txtpyyamlrichtqdmgoogle-generativeaigroqopenaiollama>=0.4.0 # Local-first supportbeautifulsoup4pypdf```
### 2. Create new file: eidolon_vault/providers/ollama.py**Create this exact file** with the code below:
```python# eidolon_vault/providers/ollama.pyimport ollamafrom typing import Dict, Any, Generator
class OllamaProvider:    """Local-first provider using Ollama. Perfect for offline use on old hardware."""
    def __init__(self, model: str = "llama3.2:3b"):        self.model = model        # Ensure model is pulled silently on first run        try:            ollama.pull(self.model)        except Exception:            pass # Already exists or network issue
    def generate(self, prompt: str, system_prompt: str = None, temperature: float = 0.7) -> str:        """Single response (non-streaming)"""        messages = []        if system_prompt:            messages.append({"role": "system", "content": system_prompt})        messages.append({"role": "user", "content": prompt})
        response = ollama.chat(            model=self.model,            messages=messages,            options={"temperature": temperature}        )        return response['message']['content']
    def stream_generate(self, prompt: str, system_prompt: str = None) -> Generator[str, None, None]:        """Streaming response (better UX for long agent runs)"""        messages = []        if system_prompt:            messages.append({"role": "system", "content": system_prompt})        messages.append({"role": "user", "content": prompt})
        stream = ollama.chat(            model=self.model,            messages=messages,            stream=True,            options={"temperature": 0.7}        )        for chunk in stream:            yield chunk['message']['content']```
### 3. Update your main LLM router / core engineFind the file that currently chooses between Gemini/Groq/OpenRouter (probably `eidolon_vault/core.py` or `agent.py`).
**Add this function** (or merge it into your existing `get_llm_provider`):
```pythonfrom eidolon_vault.providers.ollama import OllamaProvider
def get_llm_provider(provider_name: str, model: str = None):    provider_name = provider_name.lower().strip()        if provider_name == "ollama":        return OllamaProvider(model or "llama3.2:3b")    elif provider_name == "gemini":        # your existing Gemini code        ...    elif provider_name == "groq":        # your existing Groq code        ...    else:        raise ValueError(f"Unknown provider: {provider_name}")```
### 4. Add Ollama as default option in config / CLIIn your main config file or CLI parser, add support for:```yamlllm:  provider: ollama # ← new default option  model: llama3.2:3b # lightweight & fast on your 16GB RAM```
Update the CLI help text to mention: `--provider ollama`
### 5. Quick test command (add to README)After changes, user should be able to run:```basheidolon-vault run --provider ollama --model llama3.2:3b```
### Final Instructions for You (Gemini)- Apply all the above changes exactly.- Create the new `ollama.py` file.- Update requirements.txt and the main provider router.- Do not add extra features — only Ollama support for now.- After applying, output the full content of every changed/new file so I can copy-paste them directly.
**End of directives. Apply now.**```
---
